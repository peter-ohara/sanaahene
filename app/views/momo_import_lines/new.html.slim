= content_for(:top_app_bar) do
  = toolbar(navigation_icon: link_to(icon(:arrow_back), momo_import_lines_path, class: "flex items-center"))

= panel do
  == render "form", momo_import_line: @momo_import_line