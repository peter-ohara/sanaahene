= content_for(:top_app_bar) do
  = toolbar(navigation_icon: link_to(icon(:arrow_back), @non_bank_transaction, class: "flex items-center"))

= panel do
  == render "form", non_bank_transaction: @non_bank_transaction