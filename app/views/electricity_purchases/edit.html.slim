= content_for(:top_app_bar) do
  = toolbar(navigation_icon: link_to(icon(:arrow_back), @electricity_purchase, class: "flex items-center"))

= panel do
  == render "form", electricity_purchase: @electricity_purchase