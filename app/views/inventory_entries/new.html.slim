= content_for(:top_app_bar) do
  = toolbar(navigation_icon: link_to(icon(:arrow_back), inventory_entries_path, class: "flex items-center"))

= panel do
  == render "form", inventory_entry: @inventory_entry